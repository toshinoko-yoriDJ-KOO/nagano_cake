<!--kuma作成-->
<div class="container">
  <div class"low my-3">
    <div class="col-10">
<h3>ログイン</h3>
<p>会員の方はこちらからログイン</p>
<div class="text-center">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-group col-10">
    <%= f.label :メールアドレス %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>
    
    <div class="form-group col-10">
    <%= f.label :パスワード %>
    <%= f.password_field :password, autocomplete: "current-password" %>
    </div>
    
    <% if devise_mapping.rememberable? %>
    <div class="field">
    <%= f.check_box :remember_me %>
    <%= f.label :remember_me %>
    </div>
    <% end %>
    
    <div class="actions">
    <%= f.submit "ログイン" %>
    </div>
</div>
<p>会員登録がお済みでない方</p>
<%= link_to "ログイン", new_registration_path(resource_name) %>
から新規登録を行ってください。
<% end %>
    </div>
  </div>
</div>